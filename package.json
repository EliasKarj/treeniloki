{
  "name": "treeniloki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side GPX workout analyzer (Treeniloki).",
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\""
  }
}
